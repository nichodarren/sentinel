# CLAUDE.md — FIT Competition 2026 · Track IV · "SENTINEL"

> **Session-continuity / onboarding file.** Read this first, then **[PLAN.md](PLAN.md)** (the comprehensive master reference, 20 sections). This file = orientation + locked decisions + gotchas + current state.

---

## ⚡ TL;DR — where we are right now

Preparing a **Track IV (Human Health / Digital Health Intelligence)** submission for **FIT Competition 2026** (preliminary round). Problem **locked**; PLAN.md written; **all 6 decisions locked**; build policy = **full rebuild from scratch**.

> **📍 PROGRESS SNAPSHOT (2026-06-15, re-evaluated).** `notebook.ipynb` = **157 cells, 66 code, 0 err, 0 em/en dash, EXIT=0**, self-contained. **All 6 CRISP-DM sections present; §1–§5 COMPLETE, §6 is prose-only.** Maps to PLAN roadmap: **Phases 0–6 ALL ✅** (0 lock · 1 reframe/clean/EDA=§2-3 · 2 modeling RQ1+RQ2=§4 · 3 conformal RQ3=§5.3-5.4 · 4 active-testing RQ4=§5.5 · 5 equity+NSGA-II RQ5=§5.6+§5.9 · 6 global framing=§4.4.1/§5.2.1/§6.2). **🎯 ALL FOUR Track IV anchor phrases now satisfied — incl. "bio-inspired systems" which was zero-code until §5.9 NSGA-II landed (2026-06-15).** **REMAINING (2 items):** (1) **§6 Phase-8 T0** interactive `ipywidgets` prototype — NOT built, §6 is prose; (2) **Technical Report PDF 40% + Statement of Originality** — user-owned, not on disk. Minor cosmetic: §5.9 sits before §5.8 in cell order (Summary correctly last, but numbering reads 5.9-then-5.8).

**✅ Phase 1 COMPLETE + RESTRUCTURED (evidence-first) + VISUAL LAYER + CRAFT/INTERACTIVE PASS, then a PER-FIGURE POLISH PASS (2026-06-14).** `sentinel.ipynb` is a **6-phase evidence-first** notebook (**87 cells, 0 errors, 15/15 validation PASS, 7 static matplotlib figs + 3 interactive Plotly "hero" figs**, deterministic). **Visual upgrade (after a feasibility review that REJECTED migrating to D3/Observable — see [[viz-craft-polish-interactive-heroes]]):** Option F = matplotlib default + Plotly heroes (inline JS, offline-safe, + Kaleido PNG fallback). **Helpers in the design-system cell:** `add_caption()`, `savefig()` (200-dpi PNG → `eda_figs/fig01..fig10`), `show_interactive()`. **Env:** `kaleido 1.3.0` + `plotly 6.8.0` (6.0.1 was incompatible). **Then a long interactive per-figure polish pass with the user (each figure reviewed one-by-one):** shortened+centered titles, **standardised title sizes — figure/main title = 15 px bold, panel title = 13 px bold** (multi-panel panel titles sit one notch below the main; single-panel figs use 15), **takeaway captions REMOVED** (titles now carry the message), grid moved behind bars (`set_axisbelow`), legends repositioned. **Final figure set:** `fig01` leak-Dengua, `fig02` site-labelshift, `fig03` raw-labs (3×2, gap-annotated 101/250 vs 1/218), `fig04` headache-OR, `fig05` nestedness, `fig06` mask-ablation (0–1 scale), `fig07` **base-rate+CMI INTERACTIVE** (dual-legend, raw-MI line matching bar height), `fig08` LOSO-cliff (horizontal bars + **right panel = per-patient site-classifier strip plot**, replaced the old chartjunk gauge), `fig08b` **ROC overlay INTERACTIVE** (single full-width, slope panel dropped), `fig09` age+power, `fig10` **co-occurrence+missingness INTERACTIVE**. **3 heroes total** (`fig07`, `fig08b`, `fig10`). Built via `d:/tmp/build_sentinel_v2.py` (edit builder + rebuild + `nbconvert --execute`, never hand-edit .ipynb). The 6-phase structure (still 9 Tier-S/A headlines): **(1) Load · (2) Data-Quality EDA on RAW** (hazards, leakage demo col97, **site discovery+label-shift**, raw lab-distribution, duplicate audit) — each ending in a **🔧 Decision box** · **(3) Cleaning** (stateless repairs, each referencing its §2 evidence) · **(4) Signal-Discovery EDA on cleaned** (the 9 Tier-S/A headlines: nestedness+ceiling, MCAR/MAR, missingness-as-signal+ablation, base-rate-trap+CMI, **pooled-vs-LOSO cliff 0.74→0.38**, cohort/power/no-cluster) · **(5) Dataset-Readiness Validation** (15 hard asserts) · **(6) Persist**. Every EDA follows **Raw Output → Interpretation → Visualization → Takeaway** (raw tables/prints always kept; viz is an added layer via a single design-system cell). **Now N=299, 89 features** (added `symptom_burden`). **5 fixes applied during refactor:** persist `site`, add `symptom_burden`, split mask generic/confirmatory, preserve urination missingness, **bound `resp_rate` [8,80]** (a gap the §5 validation gate caught — prior code never applied the promised bound). Built via `d:/tmp/build_sentinel_v2.py` (reuses verbatim preprocessing cells from the reviewed notebook + targeted fixes).

**✅ Phase 1+ DEEP-EDA COMPLETE** — separate investigative notebook **`sentinel_eda.ipynb`** (120 cells, 0 errors, **51 labeled analyses** across 12 themes A–M, 29 figures, 22 tables, deterministic; built via `d:/tmp/build_eda.py`). Paper-grade stats throughout (Fisher/MWU/permutation + effect sizes + Wilson/bootstrap CIs + **BH-FDR** + MI/conditional-MI + φₖ + UMAP/clustering + power analysis). Produced a running **decision register** (47 insight→action items → `artifacts/eda_decision_register.csv`). **🔑 Headline new finding: a `Centre de santé` SITE variable (DO n=147 / DAFRA n=152) that Phase 1 had dropped** — severe between-site label shift (dengue 29%→9%, typhoid 17%→3%), domain-classifier separates sites at **AUC 0.99**, and symptoms-only dengue AUC **collapses 0.74 (pooled) → 0.38 (leave-one-site-out)**. Site re-exported to `artifacts/site.parquet` for Phase 2. See [[site-variable-distribution-shift]].

**✅ Phase 2 / §4 Modeling COMPLETE (2026-06-15).** `notebook.ipynb` §4 (RQ1+RQ2): BR→ECC over LightGBM, dual CV (stratified pooled-OOF + GroupKFold-by-site), reproduced the single-label failure and recovered it with multi-label. **Results:** single-label non-malarial-dengue recall **21.4%** → **BR 42.9% / ECC 35.7%** (within CI, formulation is the win). **RQ2 mask-ablation = near-null** — ⚠️ this REFUTED the prior deep-EDA hunch that "generic mask is mostly legitimate signal / dengue mask-only AUC ≈0.73": in the multi-label pooled-OOF setting the mask (generic OR confirmatory) does NOT move the dengue cell, confirmatory mask shows **no leakage**, and only `+labs` moves a cell (malaria, not dengue). The single-label AUC signal did not translate to OOF recall. See [[section4-modeling-results]]. **🧭 Positioning LOCKED: deliverable = triage/referral assistant, not detector** (moderate dengue recall = referral trigger) — see [[deployment-positioning-triage]].

**✅ Phase 3 / §5 Evaluation COMPLETE incl. extensions + 6-role review (2026-06-15).** `notebook.ipynb` now **133 cells, 0 err, 0 em/en dash, EXIT=0, 12 PNG + 4 Plotly**. Built the decisive triage section: **5.1** framing (3 criteria + fairness screen) · **5.2** site-aware LOSO (dengue pooled AUC **0.78** → pooled-transfer **0.37**, per-fold within-site ~0.58–0.60; the 0.37 is score non-comparability across districts, explained in prose) + interactive ROC overlay + **site-classifier AUC 0.99** + strip plot + **V6 phenotype map** (SVD/MCA, 34% var, symptoms track SITE not disease) · **5.3** per-label Mondrian conformal (coverage **Malaria 92% [Wilson 87,96] / Dengue 89% [72,96] n=27**, guarantee only Mal/Den) · **5.4** asymmetric referral rule (refer when dengue not ruled out → **24/27 dengue = 89%, 3 missed** vs naive-0.5 **8/27**; dengue-anchored because it is the only dangerous+weakly-separable disease the cohort can calibrate) · **5.6** fairness audit (age/sex/site, sensitivity as caught/total, U5 underpowered n=2) · **5.7** model-free missingness map (22 plotted/68 omitted, corroborates the §4.5 null) · **5.8** summary. Reuses §4 instruments verbatim; site-aware CV; malaria-first chain. **Then a 6-role read-only review** (ML-methodology, stat-rigor, clinical/epi, integrity, rubric, reproducibility): §5 judged sound+honest; **8 confirmed fixes + Clinical#1 applied & re-verified** (Wilson CIs, fairness small-cell suppression, LOSO per-fold + sign, exchangeability cross-ref, sex-gap wording, missingness-filter disclosure, SVD-sign + SEED). **Scope-creep REJECTED** (conformal-on-LOSO, flowchart, new §5.5/§5.9, ops-capacity → §6) and **2 false positives ignored** (§5.1-is-a-stub, mask-naming bug). See [[section5-evaluation-core]].

**✅ EXTERNAL-DATA INTEGRATION COMPLETE (2026-06-15) — Kenya LaBeaud + Tanzania, "ambitious" role.** After a from-scratch external-data re-research (user asked to redo it + be creative, not limited to transfer/§6 — see [[kenya-labeaud-external-dataset]]), the external-data plan was overhauled and integrated into `notebook.ipynb` (now **141 cells, 0 err, 0 em/en dash, EXIT=0**). **Chosen dataset: Kenya "Western and coastal Kenya sick visit data 2019-2022"** (Kiener/**LaBeaud**/Ndenga/Mutuku, Dryad `w9ghx3fxc`, CC, 1534×119, **dengue PCR 30+/malaria RDT 289+**) — the live successor cohort from the **same group as Mutai et al. 2023** (the "smoking gun" 0–5.5% paper; the actual Mutai dataset `rn8pk0pg1` is **DEAD/withdrawn** — 404 + 0 API versions). **User downloaded it manually** (Dryad file API needs a bearer token → curl 401) into `external_data/Western and coastal Kenya sick visit data 2019-2022/`. **Three integration points built:** **§4.4.1** = independent reproduction of the single-label failure (clinician dengue-dx sensitivity **13.3%** = 4/30; accuracy-tuned single-label ML **0.0%** = flags 0 of 878; yet symptoms carry signal, AUC **0.73** → the *framing* fails, not the data) · **§5.2.1** = cross-cohort convergence table (Kenya dengue AUC 0.73 / malaria 0.62; **Tanzania** malaria 0.71 / typhoid 0.54 → malaria moderately separable everywhere, minority/dangerous diseases weak everywhere = not a one-cohort artefact) · **§6.2** = honest multi-region generalisation roadmap (local recalibration before use, external-validation, **granular interoperable symptom capture as a precondition** — the Kenya 6-group aggregation is why no model could transfer, framed as a design lesson; federated/WHO-ethics/FUTURE-AI). **Tanzania D'Acremont kept CONCISE** (Mal/Typ convergence only; NOT a pillar — core message is dengue, which Tanzania lacks). **SISA + MAP DROPPED** (SISA has no dengue label in-file; MAP marginal). **Guardrails honoured:** Burkina cohort stays primary/evaluated; external rows NEVER merged into training/calibration/fairness; refs added (LaBeaud Dryad, D'Acremont NEJM, WHO-ethics, FUTURE-AI → also helps ≥3-journal-ref bar). **Role = "validation of FINDINGS + reproduction", NOT granular weight transfer** (Kenya symptoms pre-aggregated to 6 groups). Built via `d:/tmp/build_notebook.py`. See [[kenya-labeaud-external-dataset]].

**✅ RQ4 + NSGA-II bio-inspired COMPLETE (2026-06-15).** `notebook.ipynb` now **157 cells, 66 code, 0 err, 0 em/en dash, EXIT=0** (after absorbing 3 improvements from teammate's `notebook_derick.ipynb`: memoisation, formal `n_ieq_constr=1`, per-district DO-vs-DAFRA knee comparison Jaccard 0.16). User commissioned both (placed in §5 Evaluation; NSGA-II built ALL-OUT not committed-minimal; RQ4 enriched; pymoo guarded-import). **§5.5 VoI (RQ4)** + **§5.9 NSGA-II minimal questionnaire (RQ5/bio-inspired, fills the empty anchor pillar).** Key honesty fixes during build: AP objective (not degenerate recall-on-14), surrogate-in-search + full-ECC re-score (runtime), benchmark shows NSGA-II at best ≤k-budget member. NSGA-II wins benchmark (AP 0.450 + gap 0.095, best on both). See [[rq4-voi-nsga2-bioinspired]]. **NOTE:** this supersedes the older line-17 review note that "rejected new §5.5/§5.9 scope-creep" — that was advice against UNPROMPTED scope; user has now explicitly directed both.

**✅ §6.1 DEPLOYMENT T0 PROTOTYPE COMPLETE (2026-06-15) — roadmap Phase 8 T0.** `notebook.ipynb` now **161 cells, 69 code, 0 err, 0 em/en dash, EXIT=0**. Built the inline `ipywidgets` triage prototype into §6.1 (was prose-only): symptom checklist (12 signs = §5.9 NSGA-II most-selected) → per-disease ECC probabilities + §5.3 conformal prediction set + §5.4 referral flag + §5.5 next-test (wbc) + "NOT FOR CLINICAL USE" disclaimer. **Key fix (3 iterations): conformal qhat must be the EXACT §5.3 thresholds (reuse the `conf` dict, calibrated on OOF), NOT re-derived in-sample; typhoid/YF stay descriptive-only (not in set, no referral); worked examples scored on real patients' OOF probs.** Verified: real malaria + real non-malarial-dengue patients both correctly REFER {Malaria,Dengue}; patient B's dengue prob is 2% yet conformal keeps dengue in-set → catches a case a naive 0.5 detector discards (the §5.4 24/27-vs-8/27 story on one patient). §4/§5 headline numbers intact. See [[t0-triage-prototype]]. **User chose "build T0 first, then re-evaluate upgrade" → T1 (deployed Gradio/Streamlit + persisted model + optional HF Spaces) is the NEXT decision, NOT yet built.**

**✅ T0 UI POLISH + STATIC SNAPSHOT + TEMP NSGA-II CACHE (2026-06-15, same session, all pushed).** Three follow-ups after the T0 build, all display-only / dev-convenience (no numbers changed): **(1) static HTML snapshot** baked into the widget cell so it is never blank on an untrusted/offline notebook (user saw blank Plotly+widget = the notebook was "Not Trusted"; Trust + Run All renders the live widget). **(2) Widget UI polish** (user-requested): readable symptom labels via an override map (fixes raw `chaist_pair`-type typos + `headache_2`); the result card split into a "calibrated decision (malaria, dengue)" block and a divider-separated "shown for context only (not calibrated)" block with an explainer that YF/typhoid (n=12/29) never enter the set or trigger referral (addresses user's confusion that those two never change status); the per-label inclusion threshold shown next to each calibrated label (dengue "IN SET (kept if ≥1%)" explains why 2% is still referred); non-symptom features (demographic/derived, e.g. age_band) hidden from the "symptoms marked present" line. **(3) TEMPORARY NSGA-II result cache** (`artifacts/nsga_cache.pkl`, `FORCE_RETRAIN=False` flag, auto-fallback): caches §5.9 search results → **cached rerun ~12min → ~2.4min (~5x), all numbers identical**. **MUST be removed before final** (self-contained rule); removal steps in [[t0-triage-prototype]]. **Git: commits f496f38 (T0+cache) → 3ccc5c7 (UI polish) → c76c037 (re-exec) on `main`, pushed to github.com/nichodarren/fit-competition-2026; `report/` (Derick draft, 2.7MB) deliberately EXCLUDED from all commits.**

**Immediate next decision: T1 deployed app — or stop at T0?** (user weighed full deployment, chose T0-first). Still open (user-owned): **Technical Report PDF 40%** (Derick's EARLY DRAFT now exists at `report/report.tex` + report_assets + compiled PDF — **do NOT touch yet, user said "belum saatnya"**) **+ Statement of Originality** (not on disk). `notebook_derick.ipynb` was DELETED by the user (positives already absorbed into §5.9). Optional later: V6 interactive-toggle companion.

---

## 🧭 ACTIVE WORKSTREAM (2026-06-14) — `notebook.ipynb` = the CRISP-DM final deliverable

**Decision (locked):** the single graded notebook to submit is **`notebook.ipynb`**, structured **explicitly by the six CRISP-DM phases** (1 Business Understanding · 2 Data Understanding · 3 Data Preparation · 4 Modeling · 5 Evaluation · 6 Deployment). Phase **structure** = CRISP-DM; phase **content** = the PLAN.md roadmap/RQs. This reframes (does NOT replace) the evidence-first work: `sentinel.ipynb`/`sentinel_eda.ipynb` remain the working notebooks that `notebook.ipynb` **ports from**.

> **⚠️ NAME CLASH:** CLAUDE.md / locked-decision #5 call `notebook.ipynb` "the teammate's single-label notebook, reference-only for what to avoid." **That file does NOT exist on disk** (only `sentinel*.ipynb` exist). The name `notebook.ipynb` is now **OUR CRISP-DM deliverable**. The teammate's notebook is a concept (avoid single-label/SMOTE/over-engineering), not a file here.

**Hard rules for `notebook.ipynb`** (see [[crispdm-notebook]], [[notebook-writing-style]], [[data-understanding-design]]):
- **Self-contained** — submitted alone; NO mention of any external file (PLAN.md, report, `sentinel.ipynb`, artifacts/parquet). Cross-reference only its own sections.
- **Prose style** — formal-academic, flowing narrative, **NO em dash, NO en dash**, citations on key claims, human-not-AI. Builder asserts the dash rule.
- **Build discipline** — edit `d:/tmp/build_notebook.py` → rebuild → `jupyter nbconvert --to notebook --execute --inplace notebook.ipynb --ExecutePreprocessor.timeout=300`. Never hand-edit the .ipynb.
- **CRISP-DM boundaries (locked):** Data Understanding = **detect-only** (never mutate data; fixes deferred to §3). Signal-discovery EDA = the **model-free** analyses stay in §2.5; the **features they motivate** are built in §3 with back-references; **model-based** analyses (site-cliff classifier, LOSO, missingness-as-signal CV, clustering) are deferred to **§5 Evaluation**.

**Status (2026-06-15): Sections 1 + 2 + 3 + 4 DONE & executed** — **111 cells**, 0 errors, 16/16 readiness PASS, 0 em/en dash, 10 PNG figures, self-contained. (§1-3 detail below is unchanged from the review round; see [[review-round-2026-06-15]].) §2 ports the 5 data-quality EDAs (detect-only) + target geometry + model-free signal EDA; §3 ports the preprocessing + `age_band` → **90 features** (incl. a **`derived` group** = `symptom_burden` + `age_band`, moved out of `demographic` so RQ2 feature-regimes stay honest; `demographic`=4), N=299. **§2 figures = 10: 8 static + 2 Plotly interactive**; **§3.8 data-quality scorecard** = the Issue→Fix flagship viz. **§4 Modeling (= roadmap Phase 2, RQ1+RQ2) NEW this session:** 4.1 technique (BR→ECC, LightGBM NaN-native, malaria-first chain) · 4.2 dual CV (`iterstrat.MultilabelStratifiedKFold` pooled-OOF + `GroupKFold`-by-site; leak-free in-fold pipeline = IterativeImputer+indicator / RobustScaler vitals / within-fold rank lab_ambiguous; bootstrap CI) · 4.3 baselines (prevalence + reproduce single-label failure) · 4.4 RQ1 · 4.5 RQ2 (3 regimes + mask ablation, 2-panel fig) · 4.6 summary→§5. **NEW DEPENDENCY: `iterstrat` (iterative-stratification 0.1.9) pip-installed**; re-execution needs it. LGBM `n_jobs=1`. **§4 RESULTS:** RQ1 single-label NMD recall **21.4%** → multi-label **BR 42.9% [21.4,71.4] / ECC 35.7% [14.3,64.3]** (within CI → the win is the *formulation*; ECC carried for §5 conformal); per-disease ECC malaria F1 0.92/PR-AUC 0.94, dengue F1 0.33, YF 0.12, typhoid 0.28, macro-F1 0.41. RQ2 mask ablation = **near-null** (confirmatory mask NO leakage; generic marginal+mixed; NMD flat 0.357; only +labs moves a cell = malaria, NOT dengue). ⚠️ within-population (optimistic); honest LOSO numbers come in §5. **A pre-written "confirmatory mask = leakage" narrative was REFUTED by the data and corrected** (see [[section4-modeling-results]]). **§4 then underwent a 6-role multi-agent review (2026-06-15)** — ML-methodology, statistical-rigor, clinical/epi, research-integrity, rubric-strategy, reproducibility — verdict: foundation sound + honest; **all MUST+SHOULD fixes applied & re-verified** (guarded `iterstrat` import w/ auto-install, removed dead code + alignment asserts; **paired bootstrap CI of BR−ECC difference +7.1% [+0.0,+21.4] replacing weak overlapping-CI logic**; before/after table single-label 3/14→BR 6/14→ECC 5/14; disaster opener + triage forward-link + macro-F1/false-negative/typhoid-nested/YF caveats + Mutai 0-5.5% baseline in prose). LOSO kept in §5 (not pulled forward). Re-verified 111 cells, 0 err, 0 dash, EXIT=0. Still deferred to §5: site-cliff/LOSO + ROC overlay, full missingness-CV, conformal, fairness, clustering, V6 phenotype map — see [[deferred-figures-notebook]]. **Next: Section 5 Evaluation.**
> **🧭 DEPLOYMENT POSITIONING — LOCKED 2026-06-15: the deliverable is an UNCERTAINTY-AWARE TRIAGE / REFERRAL ASSISTANT, NOT a detector/diagnostic.** User flagged that §4 metrics look weak for a *detection* task; resolved by re-scoping (the problem statement always said triage/refer/recommend-test). **Moderate dengue recall = a referral trigger, not a failure**: "cannot rule out dengue → refer / request test" is safer than an over-confident detector that silently misses dengue. Justify "low" metrics via (1) comparison vs single-label (0-5.5% lit. → ~43%), (2) moderate dengue is a *finding* consistent with the EDA (no single symptom separates dengue; high F1 would imply leakage), (3) honesty + conformal "knows when it doesn't know". This makes **§5 (conformal + LOSO) the decisive, non-optional section.** Guardrails unchanged. See [[deployment-positioning-triage]].
> **⚠️ OPEN BLOCKERS (deferred by user; user manages time/resources):** (1) **Technical Report PDF (40% of score) + Statement of Originality do NOT exist on disk yet** — Phase-7, user-owned; (2) **`artifacts/` parquets are STALE (89-feat, no age_band)** — `notebook.ipynb` never reads/writes them (self-contained), but **Phase 2 must NOT load them**; build features in-notebook or add a persistence cell first. artifacts/ kept (not deleted) because sentinel*.ipynb load it on re-exec.

**🆕 EDA viz additions (2026-06-14, after an ideation pass — see [[eda-viz-additions-notebook]]):** built an experiment bench **`notebook_eda_experiments.ipynb`** (copy of notebook.ipynb + appended "Section 2X" with 8 ideas V1–V8, via `d:/tmp/build_eda_experiments.py`). User picked 4; **3 ported into notebook.ipynb & executed (0 err):** **V1 UpSet** (static, §2.3, red = non-malarial-dengue yardstick n=14, arrow dropped → explanation in markdown), **V3 interactive** (Plotly nullity-matrix + co-missingness, §2.5.2), **V5 interactive** (Plotly symptom co-occurrence network, hover-only, NEW **§2.5.5** "Symptoms cluster into syndromes, but none cleanly marks dengue"). This **adds Plotly to notebook.ipynb** (was 100% matplotlib): new `show_interactive()` helper = inline-JS (offline-safe) + Kaleido PNG fallback (`eda_figs/fig_missingness_structure.png`, `fig_symptom_network.png`). **V6 phenotype map (MCA; static+interactive) DEFERRED to §5 Evaluation** (not built yet) — its standout finding: symptom variation tracks **site, not disease** (a sharper restatement of [[site-variable-distribution-shift]]); code lives in the bench, port when §5 exists. V2/V4/V7/V8 rejected (stay in bench).

---

## 🗣️ How to work with this user (conventions)

- **Language:** user communicates in **Indonesian** → reply in Indonesian. Artifacts (PLAN.md, CLAUDE.md, report, code) in **English** (report MUST be English per rules).
- **Style:** creative, outstanding, memorable; genuine **research contribution** (publishable), not just a contest entry; **global-scale** framing (NOT Indonesia-specific); anchored to the **Track IV Primary Focus**; not intimidated by the short deadline (real research beyond the contest).
- **Honesty over cheerleading.** Flag overfitting/over-claiming/decorative complexity. Novelty = reframing + integration, not stacking techniques.
- User is a participant with at least one **teammate** (the registered member who wrote `notebook.ipynb`).

---

## 🎯 The project on one screen

| Field | Value |
|---|---|
| Competition | FIT Competition 2026 — Data Science, **Track IV [Health]**, UKSW Salatiga (international) |
| Theme | *Digital Impact for Humanitarian Response and Global Well-Being* |
| **Primary Focus (anchor)** | *"Utilizing **bio-inspired systems** and **advanced analytics** to mitigate the **long-term secondary impacts of disasters** on human well-being."* |
| Dataset | Mendeley `cf49v47z4c` (Ouedraogo et al., 2025) — **300 × 109**, Burkina Faso (DO & DAFRA), French, CC BY 4.0, **unbenchmarked** |
| **Deadline** | **16 June 2026, 23:59 WIB** |
| Deliverables | `.ipynb` (**60%**) + Technical Report PDF (**40%**, ≤20 pp, English, Bab I–V) + Statement of Originality |
| Scoring (notebook 60%) | viz/understanding 20% · preprocessing 20% · model performance 20% |
| Scoring (report 40%) | intro/problem/solution/contribution 10% · lit review 10% · methodology 10% · results 10% |
| Final | Top 5 → on-site 8–9 Jul (12 h live-coding + presentation 60%); ≥3 journal refs |

---

## 📁 Files (`d:\Development\competition\21 - FIT 2026`)

| File | What | Status |
|---|---|---|
| **PLAN.md** | Master plan (reviewed & corrected) | ✅ source of truth |
| **CLAUDE.md** | This onboarding file | ✅ |
| **sentinel.ipynb** | ✅ **Phase-1 notebook — evidence-first + polished visual layer** (6 phases, **87 cells**, 0 err, **15/15 validation PASS**, **7 static + 3 interactive figures**) — built via **`d:/tmp/build_sentinel_v2.py`** (reuses verbatim preproc cells + 5 fixes; **edit the builder + rebuild + `nbconvert --execute`, never hand-edit the .ipynb**). **Design-system cell** defines `add_caption()`, `savefig()` (200-dpi PNG → `eda_figs/fig01..fig10`), `show_interactive()` (Plotly inline-JS + Kaleido PNG). After the bulk pass, **each figure was polished one-by-one with the user**: short centered titles (**main/suptitle 15 px bold > panel title 13 px bold**), **captions removed** (title carries the message), grid behind bars, legends repositioned. **3 interactive Plotly heroes:** `fig07` base-rate+CMI, `fig08b` ROC overlay (single full-width), `fig10` co-occurrence+missingness. `fig08`'s right panel is a **per-patient site-classifier strip plot** (replaced a single-value gauge bar that was chartjunk). Carries the 9 Tier-S/A headlines; deep analysis in `sentinel_eda.ipynb`. | ✅ graded deliverable |
| **sentinel_eda.ipynb** | ✅ **Phase-1+ deep-EDA notebook** (120 cells, 0 err, 51 analyses A–M, 29 figs) — standalone (loads `artifacts/`), built via `d:/tmp/build_eda.py`. Paper-grade stats + decision register. Robust to 88- or 89-feature artifacts. | ✅ graded deliverable (companion) |
| *(notebook relationship — LOCKED)* | **Deliberate split, NOT to be "de-duplicated".** `sentinel.ipynb` = lean, self-contained graded deliverable carrying the **9 Tier-S/A headlines** with the evidence→decision→validation chain. `sentinel_eda.ipynb` = deep investigative **appendix** (51 analyses) that expands them. The ~9-headline overlap is intentional (each notebook is independently runnable & 0-error). Decision **(a)** chosen 2026-06-14; do not strip the overlap. | ✅ |
| **artifacts/** | `X_features.parquet` (**299×89** — from `build_sentinel_v2.py`, **STALE: no `age_band`/`derived`**), `Y_labels.parquet` (299×4), `M_missing_mask.parquet`, `others_label.parquet`, `feature_groups.json`, `issue_fix_register.csv`, **`site.parquet`** (site + dx string, N=299), **`mask_partition.json`** (generic vs confirmatory), `eda_decision_register.csv` | ⚠️ **STALE 89-feat** — `notebook.ipynb` (90-feat) does NOT use this; **Phase 2 must NOT load these parquets** (build in-notebook or add a persist cell). Kept (not deleted) only because `sentinel*.ipynb` load it on re-exec. See [[review-round-2026-06-15]] |
| **external_data/** | ✅ **3 supplementary CC-BY cohorts (acquired 2026-06-14, GuideBook FAQ #2)** — `map_burkinafaso_pf_prevalence.csv` (MAP WFS, 258 surveys 1985–2012, median pf_pr≈0.54 → enrichment prior), `zenodo_4121831_SISA_arboviral.csv` (534×30, near-mirror symptom block → dengue transfer), `tanzania_fever_full.parquet`/`.dta` (1005×749, African pediatric + malaria/typhoid labels → external-validation). Reproduction commands in **`EXTERNAL_DATA_GUIDE.md`**. **SUPPLEMENTARY-ONLY:** primary = official cohort; never merge external rows into core training / conformal calibration / fairness-power. Phase-6 transfer + feature-enrichment only. See PLAN.md §6.1 | ✅ feeds Phase 6 |
| **eda_figs/** | 14 exported PNGs from `sentinel.ipynb` (EDA-1..10,12,13,15,16). `sentinel_eda.ipynb` figs are inline only (not separately exported). | ✅ |
| **data.csv** | Dataset (300×109), French, `;`-sep, `,`-decimal, **`utf-8`** (NOT latin-1/cp1252 — see gotcha) | raw |
| **Dataset.pdf** / **GuideBook.pdf** / **desciption.xlsx** | Track desc / rules / data dictionary (64 attrs, conceptual only — NOT 1:1 to columns) | reference |
| **notebook.ipynb** | ✅ **THE CRISP-DM final deliverable.** Structured by the 6 CRISP-DM phases; content = PLAN.md roadmap. **Self-contained** (submitted alone, no external-file mentions); prose = formal-academic, **no em/en dash**. Built via **`d:/tmp/build_notebook.py`** (edit builder → rebuild → `nbconvert --execute`; never hand-edit). **Sections 1+2+3+4 done** (**111 cells**, 0 err, 16/16 readiness PASS, 0 em/en dash, 10 PNG figs, `age_band`+`symptom_burden` in a **`derived` group** → 90 feats). **§2 figures = 10 (8 static + 2 Plotly interactive)**; **§3.8 data-quality scorecard** (Issue→Fix flagship viz). **§4 Modeling (RQ1+RQ2):** BR→ECC, dual CV (`iterstrat` stratified + GroupKFold-by-site), single-label-failure reproduction, mask-ablation (near-null, corrected narrative) — see [[section4-modeling-results]]. **NEW dep `iterstrat` (0.1.9)** needed for re-execution. Honesty pass: 0-5.5% claim hedged+cited, burden cites WHO, no fabricated refs. Model-based EDA still deferred to §5 (V6 phenotype map, site-cliff/LOSO, conformal, fairness) — see [[deferred-figures-notebook]]. See [[crispdm-notebook]], [[deployment-positioning-triage]], [[notebook-writing-style]]. **NOTE:** filename = OUR deliverable, not the teammate's single-label notebook (which is not on disk). | ✅ **primary graded deliverable** |
| **notebook_eda_experiments.ipynb** | 🧪 **EDA experiment bench (2026-06-14)** — copy of notebook.ipynb + appended "Section 2X" with 8 visual ideas V1–V8 (106 cells, 0 err), built via `d:/tmp/build_eda_experiments.py`. Source for the 3 visuals ported into notebook.ipynb + the deferred V6 (and the 4 rejected ideas). **NOT a deliverable; do NOT ship.** See [[eda-viz-additions-notebook]]. | 🧪 working/experiments |
| *(teammate's notebook)* | ⚠️ Conceptual reference only (single-label label-powerset, SMOTE, over-engineering = what to AVOID). **No such file is present in this workspace.** Do NOT copy that approach. | reference-only |
| **phase1_audit_findings.md**, **PHASE1_REVIEW_BRIEF.md** | Internal working/review notes | ⚠️ **DO NOT ship** in submission bundle |

---

## 🔒 Locked decisions (the spine — all 6 confirmed)

1. ✅ **Problem statement + name "SENTINEL"** — approved (verbatim below).
2. ✅ **Scope:** RQ1–RQ3 = headline backbone (core + safety); **RQ4–RQ5 = extensions/Final**.
3. ✅ **Phase 8 (deployment):** **T0** inline `ipywidgets` demo before report is finalized; **T1** deployed Gradio/Streamlit app for the Final; framed as a **prototype** (non-clinical disclaimer).
4. ✅ **Bio-inspired = committed-minimal** — NSGA-II minimal-questionnaire designer (objectives {performance ↑, #questions ↓, fairness-gap ↓} → Pareto-front viz). **Refit inside each outer CV fold; time-boxed; benchmarked vs L1/MI.** Degrades to future-work only if Day-3 core is genuinely at risk.
5. ✅ **Build policy = FULL REBUILD FROM SCRATCH.** Teammate is a registered member (so no originality DQ), but per user's instruction we do **NOT adopt their approach or copy any code** — the teammate's notebook is reference-only for what to avoid (single-label label-powerset, SMOTE, over-engineering). Re-implement all preprocessing from the data dictionary. **⚠️ Note (2026-06-14):** the filename `notebook.ipynb` now refers to **OUR CRISP-DM deliverable**, not the teammate's (which is not present on disk) — see ACTIVE WORKSTREAM above.
6. ✅ **External data = SUPPLEMENTARY-ONLY (2026-06-14).** GuideBook FAQ #2 (wins over the blanket "no external" clause) permits external data *only as supplementary* (transfer learning / feature enrichment), open-source + legal + cited. 3 CC-BY cohorts **acquired & exported live** to `external_data/` (MAP BF prevalence / SISA arboviral / Tanzania pediatric fever). **Hard guardrails:** official Mendeley cohort stays primary/evaluated; external rows **never** in core training, conformal calibration, or fairness-power; transfer needs explicit label-mapping → {Mal,Den,Typ,YF} + honest domain-shift reporting; cite all three (also helps ≥3-journal-ref bar). Used in **Phase 6 only**. Provenance + reproduction: `external_data/EXTERNAL_DATA_GUIDE.md`; details PLAN.md §6.1.

---

## 🔧 Key corrections from the final review (already applied to PLAN.md)

- **Tooling:** use **`sklearn.multioutput.ClassifierChain` / `MultiOutputClassifier`** — **NOT `scikit-multilearn`** (unmaintained, breaks on sklearn 1.6). Hand-roll ECC (loop over random chain orders) + **hand-rolled per-label conformal** (~20 lines). `pymoo`/`MAPIE` install clean on Py3.13; `gradio`/`skmultilearn` not installed (defer gradio to post-submission).
- **Conformal:** **per-label (Mondrian)**, guarantees only for **Malaria/Dengue**; YF(12)/Typhoid(29) **descriptive only**. **Do NOT claim joint per-patient set coverage** (multi-label conformal doesn't give it).
- **Validation:** iterative-stratified K-fold; for rare classes **pool out-of-fold predictions across all 300 rows + bootstrap CIs** (not nested 5-fold → empty YF folds).
- **RQ1 yardstick:** **non-malarial dengue recall (n=14)** — NOT the inflated "149 two-disease" figure (most of that is Malaria+Others catch-all). On the 4 core diseases (N=299) the target geometry is **0=14**/1=204/2=80/3=1; Typhoid 27/29 nested in malaria.
- **RQ2 leakage:** ablate **confirmatory-test masks** (TDR/thick-smear/dengue-RDT) vs generic labs; report symptoms-only-without-mask as the honest lab-free ceiling. **No SMOTE.**
- **Score leverage:** **Viz 20% + Preprocessing 20% = a third of the score, deterministic** — invest equally; make the Issue→Fix register + missingness map into flagship visualizations.
- **Compliance:** report format is an **admin-DQ gate** (A4, TNR 12, spacing 1.15–1.5, margins T3/B3/L4/R3, Roman→Arabic pagination, file `UNIVERSITY_TEAM`). **External supplementary data IS allowed if cited** (FAQ#2). **≥3 journal refs matter even at Preliminary** (FAQ#7). See PLAN.md §14 checklist.
- **Disaster bridge:** lead every report chapter with the flood→VBD-surge mechanism (Pakistan 2022 / Brazil 2008); don't claim the model "predicts disaster impact."

---

## 🎯 Problem Statement (verbatim — keep synced with PLAN.md §4)

> **When disasters trigger secondary surges of co-circulating vector-borne febrile diseases — malaria, dengue, typhoid, and yellow fever — frontline health workers in low-resource settings must distinguish and treat them despite three obstacles: (1) near-identical, overlapping symptoms that can co-occur in one patient; (2) confirmatory lab tests that are scarce, delayed, or unaffordable precisely when surge demand peaks; and (3) existing ML models that force a single-disease label and depend on those same missing labs — systematically missing dengue and co-infections (sensitivity as low as 0–5.5%) and failing silently.**
>
> **Central question:** *Predict the co-occurring vector-borne disease profile from clinical symptoms alone — robust to (and possibly informed by) missing labs — while quantifying per-label uncertainty to safely refer dangerous cases, recommending the next confirmatory test, staying equitable across vulnerable subgroups, and offering an honest path to generalization.*

**RQs:** RQ1 co-infection multi-label · RQ2 lab-free + missingness-as-signal (with leakage ablation) · RQ3 per-label conformal safe-triage · RQ4 population-level "next test" (ext.) · RQ5 fairness audit + roadmap (ext.). Full table in PLAN.md §4.

---

## 📊 Dataset cheat-sheet (verified)

```python
import pandas as pd
df = pd.read_csv('data.csv', sep=';', decimal=',', encoding='utf-8')   # 300 × 109
```
⚠️ **ENCODING = `utf-8`** (verified at byte level: accent bytes are `c3 a9` = é). latin-1/cp1252 *look* right on headers but **mojibake data cells** → `'Négatif'`→NaN → silently collapses `tdr_positive`/`thick_smear_positive`/`fever_type_recurrent` to constants. This was a CAUGHT BUG; an early note that "cp1252 is correct" was WRONG.

**Target = MULTI-LABEL** (`Maladies diagnostiquées/<disease>`, 1/0):
- **Malaria 270 (90%)** · Dengue 56 · Typhoid 29 · Yellow fever 12 · **Chikungunya/Zika/Option 8 = 0 (drop)** · "Others" 99 (NOT a target VBD → all-negative on the 4; sensitivity-check kept-vs-excluded).
- **After dropping the 1 all-NaN-label row → N=299.** Two label geometries: *incl. Others* → 1=141/2=149/3=9; ***core 4 diseases (N=299)*** → **0=14/1=204/2=80/3=1** (NOT 0=15 — the 15 wrongly counted the all-NaN row), non-malarial dengue **n=14** (RQ1 yardstick), Typhoid 27/29 nested in malaria.
- ⚠️ **1 row (idx 158) all-NaN across every disease column** → **DROPPED → N=299** (decided). Gender has 2 missing; **age has 0 missing** (CLAUDE.md previously said 2 — wrong).

**Features:** ~79 binary symptoms (`OUI`/`NON`, nearly complete) · ~6 numeric labs + ~9 lab flags (often missing) · vitals (messy) · test results (`Positif`/`Négatif`: TDR, goutte épaisse — ⚠️ **trailing whitespace `'Négatif '`** + NaNs, strip before matching) · **9 medical-history flags** (mostly empty).

**Demographics:** age 0.1–85, **median ≈12.5** (≈13.5 only if age-fractions dropped — be consistent with the cleaning rule); gender **152 F / 146 M (2 missing)**.

**Missingness (top):** MUA 92% · capillary refill 70% · free-text "other symptoms" 69% *(col 107 — NOT the Others diagnosis target)* · BP 57% · lymphocytes 51% · hematocrit 35% · weight 35%. **91/109 cols <5% missing** → symptoms complete, labs sparse (core of RQ2).

**Hazards (all FIXED in Phase 1):** temp `3909`→39.09, pulse `124000`→null + BP-in-pulse, BP `"10976"`→109/76 + `13/8`→130/80 (cmHg), age fractions (`7/12`=7 mo), French decimals, accent-folding for `Négatif`.

---

## 🔑 Phase-1 verified findings & gotchas (NEW — carry into Phase 2)

- **HARD LEAK dropped:** col97 `Dengue (Dengua)` → 52/52 OUI are Dengue=1 (mirrors the label). Also dropped: col98 `Maladies diagnostiquées` (label concat), col107 free-text (names dx). **Never re-add these as features.**
- **Confirmatory tests** `tdr_positive`/`thick_smear_positive` (~99% predict malaria) → kept but **tagged in `feature_groups['confirmatory_test']` → MUST be ablated for RQ2**, never silently included.
- **8 zero-variance cols dropped** (constant) + **household_dengue near-inert** (2 positives, kept for transparency, ~zero modeling value).
- **Lab units = mixed within a column.** Only `platelets` cleanly recoverable (×1000 on the <1000 cluster). `hematocrit/wbc/neutrophils/lymphocytes/creatinine_elev` pool multiple analytes over ~5 orders of magnitude → tagged **`lab_ambiguous`**; Phase 2 must **rank-transform within-fold**, present NO absolute-level inference.
- **Two 'headache' cols kept separate** (Maux de tête vs Céphalée, 58% agree); **frequent-urination OR-merged** (cols 35+38, 99% agree).
- **Feature groups (88 total):** symptom 62 · vital 7 · lab 1 · lab_ambiguous 5 · demographic 4 · confirmatory_test 2 · history 7.
- **EDA honesty findings:** (1) Cramér's V is **undirected** → use **signed** prevalence-diff (EDA-9): hallmark dengue signs (tourniquet test, hemoconcentration) appear *inverted* because malaria saturates the "dengue−" group — a real confounding-by-dominant-comorbidity insight. (2) Missingness-as-signal (EDA-7) is **partly age-confounded** (BP-missing → age 4 vs 36) → frame as "associated (partly via age)", with Wilson CIs + n. (3) **Yellow fever symptoms-only is near-hopeless** (n=12, icterus 0% in YF+) → descriptive-only. (4) **Platelets do NOT separate dengue** here (honest null result).
- **Leak-discipline:** stateless row-wise repairs applied up-front (safe); **all stateful transforms (impute/scale/rank) deferred to inside CV folds** in Phase 2. Mask `M` is leak-free (`X.isna()`), concatenate as explicit features.
- **Build via `d:/tmp/build_sentinel.py`** (NotebookEdit stored literal `\n`; the generator writes real newlines). Edit the builder + rebuild + `nbconvert --execute`, don't hand-edit the .ipynb.

---

## 🗺️ Roadmap status (full detail in PLAN.md §10)

- [x] Phase 0 — Lock & audit (problem locked, PLAN.md written + reviewed/corrected, 5 decisions made)
- [x] **Phase 1 — Multi-label reframe + leak-free preprocessing from scratch + curated EDA** ✅ (N=299, 88 feats, 6 review subagents, all fixes applied & re-verified)
- [x] **Phase 1+ — Deep investigative EDA** ✅ (`sentinel_eda.ipynb`, 51 analyses, paper-grade stats; found+persisted the SITE variable; decision register → Phase 2 mandate)
- [x] **Phase 1 restructure — evidence-first 6-phase `sentinel.ipynb`** ✅ (Decision boxes drive each preproc step; §5 validation gate; 5 fixes incl. resp_rate bound it caught; N=299/89 feats)
- [x] **Phase 2 — Core Modeling (RQ1, RQ2)** ✅ (`notebook.ipynb` §4: BR→ECC, dual CV stratified+LOSO-by-site, reproduced single-label failure 21.4%, multi-label recovery BR 42.9%/ECC 35.7%, mask-ablation near-null. See [[section4-modeling-results]]) — built in-notebook (90 feats, did NOT read stale `artifacts/`)
- [x] **Phase 3 — Safety: per-label conformal + cost-sensitive abstention/referral (RQ3)** ✅ = `notebook.ipynb` §5 (133 cells, 0 err): Mondrian conformal Mal 92%/Den 89%, referral rule 24/27 dengue vs naive 8/27, site-aware LOSO + ROC + site-classifier AUC 0.99 + V6 phenotype map + fairness audit (§5.6) + missingness map (§5.7). 6-role review done, 8 fixes + Clinical#1 applied. See [[section5-evaluation-core]]
- [x] **Phase 4 — Active testing: population-level "next test" (RQ4)** ✅ = `notebook.ipynb` §5.5 (value-of-information ranking, enriched per-cost + per-subgroup/site). Malaria tests top raw info but malaria-adjusted dengue info ~0.005; **wbc carries most malaria-adjusted dengue info (0.058)** but 3× cost; no cheap test resolves dengue → supports referral. Honest: population-level only, circular-with-RQ2 stated. See [[rq4-voi-nsga2-bioinspired]]
- [x] **Phase 5 — Equity audit (DONE §5.6) + NSGA-II bio-inspired (ALL-OUT, DONE §5.9)** ✅ = `notebook.ipynb` §5.9. pymoo NSGA-II, 3 objectives (**dengue AP** not recall — recall-on-14 was degenerate; min #questions; min between-site AP gap), surrogate-in-search + full-ECC re-score, hypervolume + Jaccard stability + per-symptom freq + site-robustness + benchmark vs L1/MI/RFE + knee + 4-panel Plotly. **NSGA-II wins benchmark: AP 0.450 + fairness gap 0.095 (best on BOTH) vs L1 0.413/0.361, MI 0.392/0.368, RFE 0.419/0.123.** Stability Jaccard 0.18 / site-overlap 0.07 = LOW, reported honestly as fingerprint of weak distributed dengue signal → deliverable = method for locally-recalibrated questionnaire, not fixed list. **Fills the empty "bio-inspired" anchor pillar.** NEW dep `pymoo` (guarded import). See [[rq4-voi-nsga2-bioinspired]]
- [x] **Phase 6 — Global framing** ✅ = `notebook.ipynb` §4.4.1 (Kenya single-label-failure reproduction) + §5.2.1 (cross-cohort convergence Kenya/Tanzania) + §6.2 (multi-region generalisation roadmap: local recalibration, external-validation, granular-symptom-capture precondition, federated/WHO-ethics/FUTURE-AI). Kenya LaBeaud + Tanzania D'Acremont integrated; SISA + MAP dropped. See [[kenya-labeaud-external-dataset]]
- [ ] Phase 7 — Notebook + Technical Report polish (graded deliverables; run §14 format checklist)
- [x] **Phase 8 T0 — Deployment inline prototype** ✅ (`notebook.ipynb` §6.1 `ipywidgets` triage prototype, 161 cells 0 err; reuses §5.3 conformal thresholds + §5.4 referral + §5.5 next-test; see [[t0-triage-prototype]]). [ ] **Phase 8 T1 — deployed app** (Gradio/Streamlit + persisted model + optional HF Spaces) — user-decision pending after seeing T0.

**CRITICAL PATH (must-ship):** Phases 1–6 ALL DONE in `notebook.ipynb` §1–§6 (§6.1 T0 prototype now built). **Remaining must-ship:** Phase 7 (**Report PDF 40% + Statement of Originality — user-owned**; Derick's report.tex EARLY DRAFT exists at `report/`, do NOT edit yet). **Pre-committed to Final (droppable):** Phase-8 T1/T2 deployed app (= the open decision). (RQ4 + full RQ5/NSGA-II + Phase-8 T0 are NO LONGER deferred — all built 2026-06-15, see TL;DR.)

---

## 🚧 Honesty guardrails (non-negotiable — PLAN.md §13)

N=300, single-region, pediatric-skewed, imbalanced (YF 12; Chik/Zika 0) → **proof-of-concept + framework + roadmap, NOT a deployable clinical diagnostic.** Conformal guarantees Malaria/Dengue only. Fairness = audit only. Active testing = one-step, population-level. Triage/risk-stratification, not a "diagnostic oracle." Deployment = prototype + "not for clinical use" disclaimer. No SMOTE / no heavy stacking/HP-search at N=300.

---

## 💻 Environment

- Windows 11; PowerShell (primary) + Bash; **Python 3.13** with pandas (installed: sklearn 1.6, lightgbm, catboost, shap, streamlit, ipywidgets; install-clean: MAPIE, pymoo; NOT installed: skmultilearn, gradio).
- Workspace root: `d:\Development\competition\21 - FIT 2026`.
- Tech (planned): pandas, **scikit-learn `multioutput.ClassifierChain` (NOT scikit-multilearn)**, LightGBM, **hand-rolled per-label conformal**, SHAP, pymoo (NSGA-II), ipywidgets (T0) / Gradio (T1, later), joblib. **All transforms inside CV folds.**

---

## ▶️ Immediate next step (when user says "go")

**Phase 2 — Core modelling (RQ1, RQ2):** ⚠️ **do NOT load the stale `artifacts/*.parquet` (89-feat, no `age_band`).** Build the 90-feature matrix in-notebook (the §3 cells already produce `X`/`Y`/`M`/`site`/`feature_groups`/`mask_partition`), or add an explicit persist cell to `notebook.ipynb` first so Phase 2 reads a matching export. Then build **ECC (Ensemble of Classifier Chains)** via `sklearn.multioutput.ClassifierChain` (loop over random chain orders) on LightGBM base learners. **Three feature regimes to compare:** (a) symptoms-only, (b) +missingness mask, (c) +labs (with confirmatory-test ablation for RQ2). Reproduce the **single-label failure** (label-powerset misses non-malarial dengue, n=14). **All stateful transforms fit INSIDE iterative-stratified CV folds** (impute, RobustScaler for vitals/clean labs, **within-fold rank-transform for `lab_ambiguous`**); pool out-of-fold preds across 299 rows + bootstrap CIs for rare classes. Yardstick = **non-malarial dengue recall (n=14)**.

*Keep PLAN.md and this file updated as phases complete.*
